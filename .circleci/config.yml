version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
    -run:
      name: "install zip"
      command: |
        sudo apt-get install
        sudo apt install zip
    -run:
      name: "run ls"
      command: ls -la
      
