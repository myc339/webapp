version: 0.0
os: linux
files:
  - source: webapp
    destination: /var/webapps
  - source: scripts
    destination: /var/webapps
hooks:
  AfterInstall:
    - location: scripts/setup_spring.sh
      timeout: 180
      runas: centos
  